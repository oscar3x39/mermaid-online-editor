# Mermaid Online Editor - 專案總結

## 🎯 專案特色

### ✨ 核心功能
- **專業代碼編輯器**：使用 CodeMirror 6，支援 Mermaid 專屬語法高亮
- **即時預覽**：所見即所得的圖表渲染
- **多語系支持**：中文/英文切換
- **歷史紀錄**：自動保存編輯歷史
- **高清匯出**：PNG 格式下載
- **URL 分享**：壓縮後的分享連結

### 🛡️ 架構優勢
- **100% 離線可用**：零外部依賴，完全自託管
- **無單點故障**：不依賴任何第三方 CDN 或服務
- **隱私保護**：所有資源本地化，不向外發送請求
- **高性能**：使用系統字體，載入速度極快

## 📦 技術棧

### 前端框架
- **Vue 3** - 現代化的響應式框架
- **Vite** - 極速的構建工具
- **Mermaid.js** - 圖表渲染引擎

### 代碼編輯器
- **CodeMirror 6** - 專業級代碼編輯器
- **自訂 Mermaid 語法解析器** - 精確的語法高亮
- 支援功能：
  - 行號顯示
  - 語法高亮（Dracula 配色）
  - 自動完成
  - 括號匹配
  - 搜尋功能
  - 多游標編輯

### UI 組件
- **Lucide Vue** - 現代化圖標庫
- **自訂 GitHub Star 按鈕** - 無外部依賴
- **系統字體堆疊** - 跨平台一致性

## 🎨 設計系統

### 主題
- 深淵黑（預設）
- 陽光白
- 森林綠
- 霓虹紫

### 字體
- **Sans-serif**: -apple-system, BlinkMacSystemFont, Segoe UI, Noto Sans
- **Monospace**: ui-monospace, Cascadia Code, Source Code Pro, Menlo, Consolas

## 🚀 部署

### Docker 部署
```bash
docker-compose up -d --build
```

### 訪問
- 本地：http://localhost:8080
- 生產環境：使用 Nginx 反向代理

## 📝 開發規範

詳見 `gemini.md` 和 `.agent/SKILL.md`

### 核心原則
1. **零外部依賴** - 所有資源必須自託管
2. **離線優先** - 確保無網路環境下可用
3. **語法高亮** - 使用專屬 Mermaid 解析器
4. **多語系** - 所有 UI 文字需中英雙語

## 📊 專案結構

```
frontend/
├── src/
│   ├── components/
│   │   ├── CodeEditor.vue          # CodeMirror 編輯器
│   │   ├── GitHubStarButton.vue    # 自訂 Star 按鈕
│   │   ├── Toolbar.vue             # 頂部工具欄
│   │   ├── ShareModal.vue          # 分享彈窗
│   │   └── ...
│   ├── composables/
│   │   ├── useMermaid.js           # Mermaid 渲染邏輯
│   │   ├── useHistory.js           # 歷史紀錄管理
│   │   ├── useUrlState.js          # URL 狀態同步
│   │   └── useUndoRedo.js          # 撤銷/重做
│   ├── utils/
│   │   └── mermaidLanguage.js      # Mermaid 語法定義
│   ├── constants/
│   │   ├── examples.js             # 範例圖表
│   │   └── translations.js         # 多語系翻譯
│   └── assets/
│       └── style.css               # 全局樣式
├── public/
│   └── fonts/                      # 字體資源（預留）
└── index.html
```

## 🔧 維護指南

### 新增 Mermaid 關鍵字
編輯 `src/utils/mermaidLanguage.js`

### 新增翻譯
編輯 `src/constants/translations.js`

### 修改主題
編輯 `src/assets/style.css` 中的 CSS 變數

### 重新構建
```bash
docker-compose up -d --build
```

## 📈 未來規劃

- [ ] 即時協作編輯
- [ ] 更多圖表範例
- [ ] 主題自訂功能
- [ ] 匯出 SVG/PDF
- [ ] 語法錯誤提示

---
**最後更新**: 2026-02-13  
**版本**: 1.0.0  
**作者**: oscar3x39
